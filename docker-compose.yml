version: '3.8'

services:
  web:
    build: .
    container_name: ofcountdown-web
    restart: always
    expose:
      - "3000"
    environment:
      - PORT=3000
    networks:
      - proxy-net

networks:
  proxy-net:
    external: true
